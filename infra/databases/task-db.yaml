# The task module's database instance (D58: one instance per module).
#
# THIS FILE EXISTS BECAUSE THE CLUSTER WAS NOT REPRODUCIBLE WITHOUT IT. The
# instance and its password were created by hand in the previous cluster and were
# never committed — `git log -S "kind: MariaDB"` across deploy, task-db and argocd
# returns nothing. Everything looked healthy for as long as that cluster lived,
# and the gap surfaced only when it was rebuilt: task-db stuck in
# ContainerCreating on `secret "task-db-password" not found`, with task
# crash-looping behind it.
#
# A GitOps repository that cannot rebuild its own cluster is the failure the
# whole approach is meant to prevent, and nothing but a rebuild reveals it.
#
# NO PASSWORD IN GIT. `generate: true` has the operator mint the secret on first
# reconcile if it is absent, so the credential is created in-cluster and never
# passes through a commit. The bootstrap fields below create the instance, the
# `task` database, the `task` user and its grant in one resource — the separate
# Database/User/Grant resources are for cases this shape cannot express.
#
# THE NAMES ARE A CONTRACT WITH THE CONSUMER. task-db's chart reads
# `database.host: task-db-mariadb`, `name: task`, `user: task` and mounts
# `task-db-password` at /var/run/secrets/task-db, where the service opens the
# file `password` (DB_PASSWORD_FILE in src/main.rs). So the metadata name, the
# username, the database and the secret's KEY all have to match what is below —
# a wrong key mounts a secret whose file the service cannot find, which fails as
# a connection error rather than as a missing file.
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: task-db-mariadb
  namespace: yadgar
spec:
  # 11.8 or newer is REQUIRED, not preferred: D69's capability probe runs
  # VEC_DISTANCE_EUCLIDEAN over VEC_FromText at boot and fails the pod when the
  # engine cannot answer it. An older image starts and then refuses to serve,
  # which is the loud failure D69 wants — but pinning avoids it entirely.
  image: mariadb:11.8.8

  # TLS IS ENFORCED HERE, AND `enabled` IS WRITTEN OUT EVEN THOUGH IT IS ALREADY
  # THE DEFAULT.
  #
  # THIS ENGINE ALREADY SERVED TLS BEFORE THIS BLOCK EXISTED, and the silence in
  # these files is what hid that. mariadb-operator defaults `tls.enabled` to
  # true, so the stored spec read `{"enabled":true}` on a cluster whose manifest
  # said nothing at all. Measured 2026-09-06 against the live instance: the
  # operator had minted a per-instance CA, written `1-tls.cnf` with `ssl_cert`,
  # `ssl_key`, `ssl_ca` and `tls_version = TLSv1.3`, and issued a server leaf
  # for `CN=task-db-mariadb.yadgar.svc.cluster.local`. So reading an
  # absent `tls:` block as "no TLS" was half right, and the missing half
  # was ENFORCEMENT rather than encryption. Writing the defaulted value out
  # closes that whole class: a file that states its posture cannot be misread by
  # the next person who greps for `tls:` and finds nothing.
  #
  # `required: true` IS THE ONLY LINE THAT CHANGES BEHAVIOUR. It sets
  # `require_secure_transport = ON`, so an unencrypted connection over TCP is
  # REFUSED rather than accepted — the same line that was `false` in the
  # operator's generated `1-tls.cnf` until now. Nothing in the estate loses by
  # it: all three `-db` charts default `database.sslMode: required`, so every
  # module already negotiates TLS, and `yadgarhq/argocd` overrides none of it —
  # its `versions/*.yaml` files carry an image digest and a tag and nothing
  # else.
  #
  # THE HEALTH PROBES ARE UNAFFECTED, and that is a property of the control
  # rather than luck. `require_secure_transport` exempts unix-socket
  # connections, and the liveness, readiness and startup probes all run
  # `mariadb -u root -e "SELECT 1;"` with NO `-h`, so they reach the socket.
  # The same exemption means `kubectl exec` into this pod is NOT a test of this
  # control — a local client is exempt from it. The refusal that proves it has
  # to come over TCP from another pod.
  #
  # NO CLIENT-CERTIFICATE REQUIREMENT, AND THE ORDER IS FORCED. Requiring one is
  # expressible — `User.spec.require.x509` on the operator's own CRD — but the
  # `User` here is created by the operator from the `username:` field below and
  # is owned by this resource, and `yadgar-store` v0.2.0 carries `ssl_mode` and
  # `ssl_ca` on `PoolConfig` and NO client key or certificate. An engine that
  # demanded a certificate today would refuse every module that reaches it. The
  # engine must not require what the client cannot present, so that half waits
  # on the store crate.
  tls:
    enabled: true
    required: true

  rootPasswordSecretKeyRef:
    name: task-db-mariadb-root
    key: password
    generate: true

  # Bootstrap: the database, the user and the grant the module needs.
  database: task
  username: task
  passwordSecretKeyRef:
    name: task-db-password
    key: password
    generate: true

  # ONE replica. More than one MariaDB replica means Galera, which is a different
  # topology with its own failure modes, and D55 asks for an environment that is
  # prod-like in the ways that expose defects rather than in every way. The
  # invariant about two replicas is about the SERVICES: it exists so client-side
  # balancing and the migration race are reachable, and both are exercised by
  # task-db's two replicas pointing at one engine.
  replicas: 1

  storage:
    size: 1Gi
    # kind's bundled local-path-provisioner. Named rather than left to the
    # cluster default so the manifest says what it binds to.
    storageClassName: standard

  # Development sizing (D55). The ceiling on this machine is the number of
  # database instances, which is why each one is kept small.
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 768Mi
